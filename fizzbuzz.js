// The Logic

// Just run on the console

for (let i = 0; i <= 100; i++)
	console.log(
		((i % 3 == 0 ? 'Fizz' : '') + (i % 5 == 0 ? 'Buzz' : '') || i) + ' '
	);

//Check out the Live Demo folder for a demonstration on the browser.
